#include <string>
#include <unordermap>
#include <vector>

namespace Bkt {
    namespace utils {
        int edit_distance(std::string, std::string);
        std::wstring str2wstr(std::string);
    }
}